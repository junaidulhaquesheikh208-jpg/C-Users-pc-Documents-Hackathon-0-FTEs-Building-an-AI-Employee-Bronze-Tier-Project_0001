{
  "name": "ai-employee-server",
  "version": "1.0.0",
  "description": "Personal AI Employee Backend Server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "watch": "pm2 start orchestrator.py --name orchestrator --interpreter python3 && pm2 start server.js --name ai-server",
    "setup": "npm install && pip install -r requirements.txt"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "ws": "^8.14.2",
    "googleapis": "^128.0.0",
    "playwright": "^1.40.1",
    "node-cron": "^3.0.3",
    "winston": "^3.11.0",
    "axios": "^1.6.2",
    "body-parser": "^1.20.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}